# Copy this file to ".env.local" (git-ignored) and fill in your key.
#
# Pokémon TCG API key: get one for free at https://dev.pokemontcg.io/
# The app works without a key too, just at lower unauthenticated rate limits.
VITE_POKEMON_TCG_API_KEY=
